spring:
  
  datasource:
    url: jdbc:mysql://localhost:3306/bd_smartcity?serverTimezone=America/Argentina/Buenos_Aires
    username: root
    password: root
    

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    
    properties:
      dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false
    
  